import { defineComponent } from 'vue'

export default defineComponent({
  name: 'login',
  setup () {
    return () => {
      return (
        <div>
          login
        </div>
      )
    }
  }
})
