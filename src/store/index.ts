import { createStore } from 'vuex'

import app from './modules/app'

export default createStore({
  state: {
    test: 'zhou'
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    app
  }
})
